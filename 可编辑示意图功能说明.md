# 可编辑零部件示意图功能说明

## 📌 功能概述

本系统提供了两个版本的可编辑零部件示意图功能：

### 1. 基础版可编辑示意图 (`/editable-diagram`)
适合快速编辑和查看多种专业的零部件示意图。

**支持的专业：**
- 座椅零部件
- 灯具零部件
- 智能电子零部件
- 内饰零部件

**核心功能：**
- ✅ 拖拽移动图形
- ✅ 调整图形大小（宽度、高度、半径）
- ✅ 更改填充颜色（8种预设+自定义）
- ✅ 编辑文本标签
- ✅ 查看元素位置信息

### 2. 高级版可编辑示意图 (`/advanced-diagram`)
提供完整的汽车零部件综合示意图，包含高级编辑功能。

**包含元素：**
- 车身外观
- 前后大灯
- 车轮
- 挡风玻璃
- 车门
- 座椅
- 中控显示屏
- 方向盘

**高级功能：**
- ✅ 所有基础版功能
- ✅ 撤销/重做（无限步骤）
- ✅ 添加新元素（矩形、圆形、椭圆）
- ✅ 复制/粘贴元素
- ✅ 删除元素
- ✅ 图层管理（置顶、置底、上移、下移）
- ✅ 透明度控制
- ✅ 导出JSON数据

## 🎨 使用方法

### 基础操作流程

1. **进入编辑模式**
   ```
   点击页面顶部的"进入编辑模式"按钮
   ```

2. **选择元素**
   ```
   点击任意图形，右侧会显示控制面板
   ```

3. **移动元素**
   ```
   在编辑模式下，直接拖拽图形即可移动
   ```

4. **调整大小**
   ```
   使用右侧面板的滑块调整：
   - 矩形：宽度和高度
   - 圆形：半径
   - 椭圆：X半径和Y半径
   ```

5. **更改颜色**
   ```
   - 点击预设颜色快速更改
   - 使用颜色选择器自定义颜色
   ```

6. **编辑文本**
   ```
   点击右侧面板的"编辑"按钮，修改文本后保存
   ```

7. **保存更改**
   ```
   完成编辑后点击"保存更改"按钮
   ```

### 高级功能使用

#### 撤销/重做
```
- 点击"↶ 撤销"按钮或使用 Ctrl+Z
- 点击"↷ 重做"按钮或使用 Ctrl+Y
```

#### 添加新元素
```
1. 点击"➕ 添加图形"按钮
2. 选择要添加的形状类型：矩形、圆形或椭圆
3. 新元素会出现在画布中央
4. 拖拽到需要的位置并调整大小
```

#### 复制/粘贴
```
1. 选中要复制的元素
2. 点击"📋 复制"按钮
3. 点击"📄 粘贴"按钮
4. 新元素会在原位置偏移显示
```

#### 删除元素
```
1. 选中要删除的元素
2. 点击"🗑️ 删除"按钮
3. 元素立即从画布中移除
```

#### 图层管理
```
调整元素的显示层次：
- ⬆️ 置顶：移到最上层
- ⬇️ 置底：移到最下层
- ↑ 上移：向上移动一层
- ↓ 下移：向下移动一层
```

#### 透明度控制
```
使用透明度滑块调节元素的不透明度（0-100%）
可以创建半透明效果和层叠视觉效果
```

#### 导出数据
```
1. 点击"导出JSON"按钮
2. 系统会下载包含所有元素数据的JSON文件
3. 可用于备份、分享或导入其他系统
```

## ⌨️ 快捷键

高级版支持以下快捷键：

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+Z` | 撤销上一步操作 |
| `Ctrl+Y` | 重做下一步操作 |
| `Ctrl+C` | 复制选中的元素 |
| `Ctrl+V` | 粘贴已复制的元素 |
| `Delete` | 删除选中的元素 |
| `Esc` | 取消当前选择 |

## 📊 数据格式

### 元素数据结构

每个图形元素包含以下属性：

```typescript
interface ShapeElement {
  id: string;              // 唯一标识符
  type: 'rect' | 'circle' | 'ellipse' | 'path';  // 形状类型
  x: number;               // X坐标
  y: number;               // Y坐标
  width?: number;          // 宽度（矩形）
  height?: number;         // 高度（矩形）
  rx?: number;             // X半径（椭圆）
  ry?: number;             // Y半径（椭圆）
  radius?: number;         // 半径（圆形）
  fill: string;            // 填充颜色
  stroke: string;          // 边框颜色
  strokeWidth: number;     // 边框宽度
  text: string;            // 文本标签
  textX?: number;          // 文本X坐标
  textY?: number;          // 文本Y坐标
  d?: string;              // 路径数据（path类型）
  rotation?: number;       // 旋转角度
  opacity?: number;        // 透明度（0-1）
}
```

### 导出的JSON格式

```json
[
  {
    "id": "headlight-left",
    "type": "ellipse",
    "x": 620,
    "y": 280,
    "rx": 40,
    "ry": 30,
    "fill": "#fef3c7",
    "stroke": "#374151",
    "strokeWidth": 2,
    "text": "前灯",
    "textX": 620,
    "textY": 285,
    "opacity": 1
  },
  ...
]
```

## 🎯 应用场景

### 1. 教学演示
- 创建清晰的零部件结构图
- 实时调整示意图以配合讲解
- 标注重点部件和材料

### 2. 技术文档
- 生成标准化的零部件示意图
- 导出数据用于文档生成
- 保持视觉风格一致性

### 3. 设计评审
- 快速修改和调整设计方案
- 比较不同的布局方案
- 记录修改历史

### 4. 学习笔记
- 学生自己创建理解图
- 标注重要知识点
- 整理学习内容

## 💡 最佳实践

### 1. 命名规范
给元素使用有意义的ID和文本标签，便于后续管理：
```
✅ 好的命名：headlight-left, door-front, seat-driver
❌ 避免：rect-1, shape-2, element-3
```

### 2. 颜色使用
- 相同类型的零部件使用相同或相近的颜色
- 重要部件使用高对比度颜色
- 背景元素使用低透明度

### 3. 图层组织
- 背景元素放在底层
- 重要元素放在上层
- 文本标签始终保持在最上层

### 4. 定期保存
- 完成重要修改后及时保存
- 使用导出功能创建备份
- 保留不同版本的JSON文件

## 🔧 技术实现

### 核心技术栈
- **React**: UI框架
- **TypeScript**: 类型安全
- **SVG**: 矢量图形渲染
- **Tailwind CSS**: 样式系统

### 关键特性
- **响应式设计**: 适配不同屏幕尺寸
- **实时预览**: 所有修改即时显示
- **历史管理**: 撤销/重做功能
- **数据持久化**: 支持导出/导入

## 📝 后续计划

- [ ] 添加键盘快捷键支持
- [ ] 实现拖拽添加元素
- [ ] 支持自定义形状模板
- [ ] 添加网格对齐功能
- [ ] 实现分组和锁定功能
- [ ] 支持图片导出（PNG/SVG）
- [ ] 添加协同编辑功能
- [ ] 实现自动布局算法

## 🐛 问题反馈

如果您在使用过程中遇到任何问题或有功能建议，请联系开发团队。

---

**版本**: 1.0.0
**更新日期**: 2026-03-02
**开发者**: Claude Code
