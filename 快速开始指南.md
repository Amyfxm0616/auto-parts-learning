# 🚀 快速开始 - 可编辑零部件示意图

## 3分钟上手指南

### 第一步：访问页面

打开浏览器访问以下任一地址：

```
基础版：http://localhost:5000/editable-diagram
高级版：http://localhost:5000/advanced-diagram
```

或从首页点击"全新交互功能"区域的卡片进入。

### 第二步：选择专业（仅基础版）

基础版支持4个标签页，点击切换：
- 📍 座椅零部件
- 💡 灯具零部件
- 🖥️ 智能电子
- 🪑 内饰零部件

### 第三步：进入编辑模式

点击页面顶部的 **"进入编辑模式"** 按钮

### 第四步：选择并编辑元素

1. **移动元素**
   ```
   点击图形 → 按住拖拽 → 放到新位置
   ```

2. **调整大小**
   ```
   点击图形 → 右侧面板 → 拖动"宽度/高度/半径"滑块
   ```

3. **更改颜色**
   ```
   点击图形 → 右侧面板 → 点击预设颜色或使用颜色选择器
   ```

4. **编辑文本**
   ```
   点击图形 → 右侧面板"文本内容"旁的"编辑"按钮 → 修改 → 保存
   ```

### 第五步：使用高级功能（仅高级版）

#### 添加新元素
```
点击"➕ 添加图形" → 选择形状（矩形/圆形/椭圆）
```

#### 复制/粘贴
```
选中元素 → "📋 复制" → "📄 粘贴"
```

#### 调整图层
```
选中元素 → 右侧面板"图层顺序"区域 → 点击按钮
⬆️ 置顶 / ⬇️ 置底 / ↑ 上移 / ↓ 下移
```

#### 撤销/重做
```
点击顶部 "↶ 撤销" 或 "↷ 重做" 按钮
```

#### 导出数据
```
点击 "导出JSON" → 自动下载文件
```

### 第六步：保存更改

完成编辑后，点击 **"保存更改"** 按钮

## 💡 5个快速技巧

### 1. 快速更改多个元素的颜色
```
选中元素A → 改颜色 → 选中元素B → 再选同样颜色
（颜色选择器会记住最近使用的颜色）
```

### 2. 精确对齐
```
使用右侧面板的位置信息（X, Y坐标）
拖拽到大概位置后，可以通过观察坐标微调
```

### 3. 创建层叠效果
```
使用透明度控制 + 图层管理
将背景元素设置较低透明度并置底
前景元素保持100%透明度并置顶
```

### 4. 快速复制多个相似元素
```
创建一个标准元素 → 复制 → 粘贴 → 调整位置
重复"粘贴 → 调整位置"步骤
```

### 5. 避免误操作丢失工作
```
重要修改后立即点击"保存更改"
高级版可以使用"导出JSON"创建备份
```

## 📱 实际应用示例

### 示例1：创建学习笔记

**目标**：标注重点零部件

1. 进入基础版，选择"座椅零部件"
2. 进入编辑模式
3. 将重点部件（如"头枕"）改为醒目颜色（红色或黄色）
4. 编辑文本，添加笔记（如"头枕 - 聚氨酯泡沫"）
5. 保存更改

### 示例2：对比不同配色方案

**目标**：尝试不同的视觉风格

1. 进入高级版
2. 进入编辑模式
3. 复制所有要对比的元素
4. 第一组：使用暖色系（红、橙、黄）
5. 撤销到原始状态
6. 第二组：使用冷色系（蓝、绿、紫）
7. 使用撤销/重做在两个方案间切换比较
8. 确定方案后保存

### 示例3：创建演示材料

**目标**：制作教学用的示意图

1. 进入基础版或高级版
2. 进入编辑模式
3. 调整元素位置，优化布局
4. 为不同类型的零部件使用区分色
   - 金属件 → 灰色系
   - 塑料件 → 蓝色系
   - 软材料 → 橙色系
5. 编辑文本标签，添加材料名称
6. 导出JSON保存（高级版）
7. 截图或打印使用

## ⚠️ 常见问题

### Q: 点击图形没有反应？
**A**: 确保已经点击了"进入编辑模式"按钮

### Q: 拖拽时图形跳动？
**A**: 正常现象，这是鼠标坐标转换到SVG坐标系的效果，慢速拖拽会更平滑

### Q: 修改后想恢复原样？
**A**:
- 基础版：刷新页面
- 高级版：点击"↶ 撤销"按钮

### Q: 如何保存我的作品？
**A**:
- 基础版：点击"保存更改"（控制台查看）
- 高级版：点击"导出JSON"下载文件

### Q: 导出的JSON文件怎么用？
**A**: 这是所有元素的数据，可以：
- 分享给他人
- 备份保存
- 导入其他系统（需要开发支持）

## 🎯 学习路径建议

### 初学者（5-10分钟）
1. 先玩基础版
2. 尝试所有4个专业
3. 练习移动、改颜色、编辑文本
4. 熟悉界面和操作

### 进阶用户（10-20分钟）
1. 进入高级版
2. 尝试添加新元素
3. 练习图层管理
4. 使用撤销/重做功能
5. 导出一个JSON文件

### 专业用户（20+分钟）
1. 创建完整的示意图
2. 使用透明度创建层叠效果
3. 复制粘贴创建复杂布局
4. 导出并整理JSON数据
5. 创建自己的零部件库

## 📞 需要帮助？

- 查看详细文档：`可编辑示意图功能说明.md`
- 查看功能总结：`可编辑示意图功能完成总结.md`
- 在页面上实验：所有操作都可以撤销，放心尝试！

---

**提示**：最好的学习方法就是动手试试！打开页面，开始你的创作吧！ 🎨
